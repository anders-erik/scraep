import test from "node:test";
import assert from "node:assert/strict";

import { get_available_script_names } from "../src/scriptrunner";
// const avail = require("../back/scriptrunner.js");

test("list_available_scripts", async () =>
{

    // const scripts = await get_available_script_names();

    assert.ok(true);
    // assert.equal(scripts.length > 0, "Should have at least one available script");

});